# Segmentation Configs

Reserved for segmentation provider settings and thresholds.

Current segmentation modes are configured through CLI flags:

- `--segmentation none`
- `--segmentation mask --mask PATH`
- `--segmentation auto`
